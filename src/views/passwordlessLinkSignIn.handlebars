<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Passwordless Link Sign In</title>
</head>
<body>
    <p>{{ greetings }}</p>
    <p>{{ passwordLessIntro }}</p>
    <p><a href="{{ link }}">{{ signIn }}</a></p>
    <p>{{ passwordLessIgnore }}</p>
    <p>
        {{ thankYou }}
        <br>{{ sws2appsTeam }}
    </p>
    <p style="margin-top: 20px; border-top: 1px solid; padding-top: 20px">{{ passwordLessLinkFull }}</p>
    <p>{{ link }}</p>
</body>
</html>